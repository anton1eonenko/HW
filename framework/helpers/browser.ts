import { BasePage } from "../base/basePage";
import { expect, Locator, Page } from '@playwright/test';
class BrowserHelper extends BasePage {
    static page: any;
    public static closeTab() {
        
    }
}

export {BrowserHelper}